# 个人商业助手 V1.0 — 产品说明书

版本：V1.0
编制日期：2026-07-23
适用范围：美妆行业进销存 + 会员管理

---

## 1. 产品概述

个人商业助手是为美妆行业微商及个体工商户打造的"进销存 + 会员管理"一体化轻量工具。支持商品管理、库存追踪、销售开单、会员积分、兑换券管理、发货追踪、数据报表等功能。

**核心特点：**
- 移动优先设计，手机端为核心操作终端，PC 端大屏管理
- 本地部署，数据存于本地 SQLite 数据库，打开即用
- 支持 PWA 离线操作、USB 扫码枪
- 自动备份、异常断电数据恢复

---

## 2. 环境要求与部署

### 2.1 运行环境

| 组件 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 |
| Python | 3.11+ |
| Node.js | 18+ |
| 数据库 | SQLite（自动创建，无需安装） |

### 2.2 首次部署

**方式一：一键部署（推荐）**

双击 `business-helper/run_deploy.bat`，自动完成前端构建、数据库迁移、服务启动。

**方式二：手动部署**

```bash
# 1. 安装 Python 依赖
cd business-helper/backend
pip install -r requirements.txt

# 2. 安装前端依赖并构建
cd ../frontend
npm install
npm run build

# 3. 导入商品数据（首次）
cd ../backend
# 将商品 xlsx 文件放入 backend/data/ 目录
python scripts/build_db.py

# 4. 启动服务
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

浏览器打开 `http://localhost:8000`。

### 2.3 日常启动

**生产模式：** 双击 `run_deploy.bat`

**开发模式：**
```bash
# 终端 1：后端
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 终端 2：前端
cd frontend
npx vite --host 0.0.0.0 --port 5173
```
浏览器打开 `https://localhost:5173`

### 2.4 局域网访问

确认本机 IP（`ipconfig`），其他设备浏览器访问 `http://[本机IP]:8000`。

---

## 3. 功能模块

### 3.1 商品管理

**路径：** 左侧导航 → 商品管理

| 功能 | 操作说明 |
|------|----------|
| 查看列表 | 表格展示全部商品，支持分页滚动 |
| 筛选 | 顶部标签栏按品牌、分类筛选 |
| 搜索 | 搜索框输入商品名称关键字 |
| 新增商品 | 点击"新增商品"，填写名称、品牌、规格、售价等 |
| 编辑商品 | 点击行右侧"编辑"按钮 |
| 上架/停售 | 点击"停售/上架"按钮切换状态 |
| 产品编码 | 新增时自动生成 `BH` + 8 位数字编码，编辑页可手动修改 |
| 条码打印 | 有机编码的商品点击"打印"生成条码图片 |
| SKU 编码 | 自动生成品牌前缀 + 序号（如 `HLN-0001`） |

### 3.2 库存管理

**路径：** 左侧导航 → 库存管理

| 功能 | 操作说明 |
|------|----------|
| 查看库存 | 表格展示全部商品库存、状态（正常/预警/缺货） |
| 品牌筛选 | 动态加载所有品牌，点击筛选 |
| 库存日志 | 点击"日志"查看该商品全部出入库流水 |
| 盘点调整 | 点击"盘点"，对比系统库存输入实际数量，差异自动生成调整记录 |
| 采购入库 | 在销售开单页录入采购单 |
| 扫码入库 | 扫描条码直接入库，不关联采购单 |

### 3.3 销售管理

**路径：** 左侧导航 → 销售开单

| 功能 | 操作说明 |
|------|----------|
| 扫码出库 | USB 扫码枪扫描商品条码，自动添加到待出库列表 |
| 手动选择 | 输入商品名称搜索添加 |
| 选择会员 | 底部"选择"按钮，搜索姓名/手机号选取会员 |
| 确认出库 | 点击"确认出库"，扣除库存、关联会员积分 |
| 查看订单 | 点击单据编号弹出订单详情（商品明细、会员、金额） |
| 按月份查询 | 已出库清单顶部 ◀▶ 切换月份 |
| 退货 | 点击订单"退货"按钮，库存恢复、会员积分扣回 |

### 3.4 会员管理

**路径：** 左侧导航 → 会员管理

| 功能 | 操作说明 |
|------|----------|
| 查看列表 | 表格展示会员信息、赫莲娜积分、娇兰积分 |
| 标签筛选 | 按会员标签快速筛选 |
| 搜索 | 搜索姓名或手机号 |
| 新增/编辑 | 点击按钮新增或编辑会员信息 |
| 积分调整 | 会员详情 → 积分调整，选择品牌输入变化量 |
| 批量下发积分 | 勾选多个会员 → "批量下发积分" |
| 批量下发兑换券 | 勾选多个会员 → "批量下发兑换券" |

### 3.5 兑换券管理

**路径：** 左侧导航 → 兑换券

| 功能 | 操作说明 |
|------|----------|
| 查看列表 | 表格展示全部兑换券，含会员、到期日、剩余天数 |
| 按品牌筛选 | 赫莲娜 / 娇兰 |
| 按状态筛选 | 有效 / 已兑换 / 已过期 |
| 标记已兑换 | 点击"兑换"按钮，记录兑换时间 |
| 到期提示 | 30 天内到期显示橙色，已过期显示红色背景 |

### 3.6 发货管理

**路径：** 左侧导航 → 发货管理

| 功能 | 操作说明 |
|------|----------|
| 查看列表 | 按状态筛选未发货/已发货记录 |
| 录入快递 | 点击编辑，填写快递公司和单号 |
| 标记已发货 | 编辑时将状态改为"已发货" |

### 3.7 报表

**路径：** 左侧导航 → 报表

| 功能 | 说明 |
|------|------|
| 销售日报 | 近 30 天每日销售额、毛利趋势图 |
| 热销排行 | 商品销量 Top 10 |
| 入库报表 | 入库笔数和数量统计 |
| 库存排行 | 当前库存 Top 10 |

### 3.8 设置

**路径：** 左侧导航 → 设置

| 功能 | 说明 |
|------|------|
| 备份管理 | 手动创建备份、查看备份列表、从备份恢复 |
| 自动备份 | 开启后可设间隔小时数，定时自动备份 |

---

## 4. 数据安全

### 4.1 数据库位置

所有数据存储在 `backend/data/business.db`，SQLite 单文件格式。

### 4.2 备份策略

| 方式 | 操作 |
|------|------|
| 手动备份 | 设置页 → 创建备份，文件存于 `backend/data/backups/` |
| 自动备份 | 设置页 → 开启自动备份，按小时间隔执行 |
| 恢复备份 | 设置页 → 选择备份文件恢复，恢复前自动备份当前库 |

### 4.3 异常恢复

SQLite WAL 模式支持强杀进程后自动恢复数据。测试验证连续 10 次异常关闭均正常恢复，无数据丢失。

### 4.4 安全防护

| 防护项 | 说明 |
|------|------|
| SQL 注入 | 参数化查询，输入转义 |
| 本机认证 | 局域网写操作需 LOCAL_TOKEN，打印机启动时显示 |
| CORS | 仅允许本地和局域网白名单域名 |
| 负库存 | 开单前检查库存，不足拒绝操作 |

---

## 5. 附录

### 5.1 端口说明

| 服务 | 生产模式 | 开发模式 |
|------|----------|----------|
| 后端 API | 8000 | 8000 |
| 前端页面 | 8000（后端托管） | 5173（Vite 开发服务器） |

### 5.2 API 文档

启动后访问 `http://localhost:8000/docs` 查看 Swagger 文档。

### 5.3 验收状态

| 类别 | 结果 |
|------|:--:|
| 核心业务 | 38 项人工验证全部通过 |
| 数据一致性 | 6 项 SQL 检查 0 偏差 |
| 安全测试 | 8/8 通过 |
| WAL 恢复 | 异常断电不丢数据 |
| 备份恢复 | 手动备份可用 |
